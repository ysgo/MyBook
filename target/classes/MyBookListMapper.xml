<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="resource.MyBookListMapper">

 	<!--rownum을 이용하여 id시퀀스 번호만큼 추출  -->
 	<select id="selectAllBookList" parameterType="model.MyBookList"  resultType="model.MyBookList">
    	select id, image, m_title, registdate, m_star, m_content from 
    	(select id, image, m_title, registdate, m_star, m_content, rownum rnum from 
    	(select * from mybooklist where email = #{email} order by id asc)) 
    	where rnum between #{start} and #{last}
 	</select>
 	
  	<!-- <select id="selectAllBookList" parameterType="model.MyBookList" resultType="model.MyBookList">
    	select id, image, m_title, registdate, m_star, m_content from mybooklist where email = #{email} order by id asc
 	</select> -->

	<select id="totalCnt" parameterType="String" resultType="int">
		select count(*) from mybooklist where email=#{email}
	</select>

  	<insert id="insertBookList"  parameterType="model.MyBookList">
  		<selectKey resultType="int" keyProperty="id" order="BEFORE">
      		select mybooklist_seq.nextval from dual     
    	</selectKey>  
    		insert into mybooklist(id, email, title, author, publisher, image, m_title, m_star, m_content,  registdate) values(#{id},#{email},#{title},#{author},#{publisher},#{image},#{m_title}, #{m_star}, #{m_content}, sysdate)
	</insert>	
	
	<delete id="deleteBookList" parameterType="model.MyBookList">
		delete from mybooklist where id = #{id} 
	</delete>
	
	<update id="updateBookList" parameterType="model.MyBookList">
		update mybooklist set 
				m_title=#{m_title}, 
				m_star=#{m_star}, 
				m_content=#{m_content}, 
				registdate=sysdate
		where id=#{id}
	</update>
</mapper>